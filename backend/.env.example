# Database Configuration
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=deskbird
DATABASE_PASSWORD=deskbird123
DATABASE_NAME=deskbird_db

# To use SQLite instead of PostgreSQL (no Docker required):
# Uncomment the following line and comment out the PostgreSQL settings above
# DATABASE_TYPE=sqlite
# DATABASE_NAME=deskbird.sqlite

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=1d

# Application Port (optional, defaults to 3000)
# PORT=3000